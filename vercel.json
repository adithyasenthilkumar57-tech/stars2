{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "cd clearway-ai && npx -y pnpm install --no-frozen-lockfile",
  "buildCommand": "cd clearway-ai && npx -y pnpm install --no-frozen-lockfile && npx -y pnpm run build && node -e \"const fs=require('fs'); if(fs.existsSync('dist/public')){fs.cpSync('dist/public', '../dist', {recursive:true});}\"",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
